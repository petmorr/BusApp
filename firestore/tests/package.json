{
  "name": "supporters-bus-firestore-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore security-rule integration tests against the Firebase emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project supporters-bus-test 'jest --runInBand --ci'",
    "test:local": "firebase emulators:exec --only firestore --project supporters-bus-test 'jest --runInBand'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.30",
    "firebase": "^10.13.2",
    "firebase-tools": "^13.20.2",
    "jest": "^30.3.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  }
}
