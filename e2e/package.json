{
  "name": "supporters-bus-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests against the full Firebase emulator stack (auth + firestore + functions).",
  "scripts": {
    "build:functions": "npm --prefix ../functions run build",
    "test": "npm run build:functions && firebase emulators:exec --only auth,firestore,functions --project supporters-bus-e2e --config firebase.json 'jest --runInBand --ci'",
    "test:local": "npm run build:functions && firebase emulators:exec --only auth,firestore,functions --project supporters-bus-e2e --config firebase.json 'jest --runInBand'"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^25.6.0",
    "firebase": "^10.13.2",
    "firebase-admin": "^12.6.0",
    "firebase-tools": "^13.20.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  }
}
