# Loaded by the Firebase CLI when running with --project supporters-bus-e2e
# (the E2E test project). The emulator does not mint App Check tokens, so
# the callables must accept calls without one — production deploys NEVER
# set this.
BYPASS_APP_CHECK=true
